// ===== ИНТЕРАКТИВНЫЕ ВИДЖЕТЫ =====

class WidgetManager {
    constructor() {
        this.initializeWidgets();
    }

    initializeWidgets() {
        this.initScholarshipCalculator();
        this.initDocumentsChecklist();
        this.initBudgetPlanner();
        this.initImportantDates();
        this.initUserTypeSelector();
        this.initProgressCounter();
    }

    initScholarshipCalculator() {
        // Код для инициализации калькулятора стипендий
    }

    initDocumentsChecklist() {
        // Код для инициализации контрольного списка документов
    }

    initBudgetPlanner() {
        // Код для инициализации планировщика бюджета
    }

    initImportantDates() {
        // Код для инициализации важных дат
    }

    initUserTypeSelector() {
        // Код для инициализации селектора типа пользователя
    }

    initProgressCounter() {
        // Код для инициализации счетчика прогресса
    }
}

// Перенесено из js/widgets.js
